out
dist
release
TimeTracker-handoff
package-lock.json
pnpm-lock.yaml
tsconfig.json
tsconfig.*.json
# Markdown docs (charter, plan, spine, handoff) are hand-formatted prose with
# intentional line breaks — keep `npm run format` from reflowing them.
*.md
